# Core
anthropic>=0.40.0
langgraph>=0.2.0
langchain-core>=0.3.0
pydantic>=2.7.0
python-dotenv>=1.0.0

# Local browser UI (optional; the CLI is the primary interface)
gradio>=6.0,<7.0

# Hosted mode (optional; see DEPLOYING.md). Also a transitive dependency of
# gradio, so installing it explicitly only pins what is already there.
fastapi>=0.115
uvicorn[standard]>=0.30

# Container execution: the eval harness always, hosted mode by default
docker>=7.0.0

# Tests
pytest>=8.0.0
pytest-asyncio>=0.24.0
