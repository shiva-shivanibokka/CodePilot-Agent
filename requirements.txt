# Core
anthropic>=0.40.0
langgraph>=0.2.0
langchain-core>=0.3.0
pydantic>=2.7.0
python-dotenv>=1.0.0

# Local browser UI (optional; the CLI is the primary interface)
gradio>=6.0,<7.0

# Hermetic sandbox for the eval harness (optional)
docker>=7.0.0

# Tests
pytest>=8.0.0
pytest-asyncio>=0.24.0
